import "std/io.vix"
import "std/mem.vix"
fn main() ->i32
{
    a = malloc(10)
    for (i in 0 .. 10)
    {
        a[i] = i * i
    }
    for (i in 0 .. 10)
    {
        printf("%d ", a[i])
    }
    printf("\n")
    free(a)
    return 0
}