extern "C" {
    fn printf(format: ptr,  .. .) ->  i32
    fn puts(s: ptr) ->  i32
}
fn main(argc: i32, argv: ptr) ->  i32 {
    printf("Hello World! argc = %d\n", argc)
    for (i in  1 ..  argc) {
        puts(argv[i])
        printf("argv[%d] = %s\n", i, argv[i])
    }
    return  0
}
